// #{NAME}
// by #{AUTHOR}
// #{DESCRIPTION}
#ifdef __unix__
#include <Piduino.h>  // All the magic is here ;-)
#endif



void setup() {
  // Place your setup code here, to run once:
  // e.g. pinMode(LED_BUILTIN, OUTPUT); // Initialize the LED_BUILTIN pin as an output
  
}

void loop () {
  // Press Ctrl+C to abort ...

  // Place your main code here, to run repeatedly:
  // e.g. digitalWrite(LED_BUILTIN, HIGH); // Turn the LED on (HIGH is the voltage level)
  // e.g. delay(1000); // Wait for a second
  // e.g. digitalWrite(LED_BUILTIN, LOW); // Turn the LED off by making the voltage LOW
  // e.g. delay(1000); // Wait for a second
}
